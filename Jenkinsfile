pipeline {
    agent {
        label{
            label "built-in"
            customWorkspace "/mnt/master"
        }
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
